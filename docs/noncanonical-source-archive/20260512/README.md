# Noncanonical Source Archive

This directory contains legacy source snippets moved out of the repository root during run `GLOSS_AIDENS_HARDENING_P0_20260512`.

Active product source lanes are:

- Frontend: `src/`
- Backend: `src-tauri/src/`
- Backend crate: `src-tauri/Cargo.toml`

Archived trees in this directory are not release, CI, or patch targets. Do not use them as implementation sources unless a future migration deliberately reintroduces them through reviewed source-basis documentation.
